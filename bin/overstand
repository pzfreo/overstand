#!/usr/bin/env bash
# Overstand CLI wrapper - finds tsx from node_modules
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT_DIR="$(dirname "$SCRIPT_DIR")"
exec "$ROOT_DIR/node_modules/.bin/tsx" "$ROOT_DIR/src-ts/cli/cli.ts" "$@"
